package com.example.ledger.model;

public enum TransactionType {
    DEPOSIT,     // money into the account (savings) / payment (credit card)
    WITHDRAWAL   // money out of the account (savings) / charge (credit card)
}